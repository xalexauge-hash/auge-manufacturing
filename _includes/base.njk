<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | {{ company.name }}</title>
    <link rel="stylesheet" href="/public/style.css">
    <!-- Add favicon here -->
    <link rel="icon" type="image/png" href="/public/browser_ico.png">
    <style>
        /* Minimaler Zusatz, um das Bild einzupassen */
        .logo img {
            height: 200px;
            width: auto;
            vertical-align: middle;
        }
        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">
            <a href="/">
                <img src="/public/auge_manufacturing_logo.png" alt="AUGÉ MANUFACTURING">
            </a>
        </div>
    </header>

    <nav>
        <div class="menu-toggle" onclick="toggleMenu()">☰ Menü</div>
        <div class="nav-links" id="navLinks">
            <a href="/" class="{% if page.url == '/' %}active{% endif %}">
                Home
            </a>
            <a href="/ueber-uns/" class="{% if page.url == '/ueber-uns/' %}active{% endif %}">
                Über mich
            </a>
            <a href="/leistungen/" class="{% if page.url == '/leistungen/' %}active{% endif %}">
                Leistungen
            </a>
            <a href="/maschinenpark/" class="{% if page.url == '/maschinenpark/' %}active{% endif %}">
                Maschinenpark
            </a>
            <a href="/kontakt/" class="{% if page.url == '/kontakt/' %}active{% endif %}">
                Kontakt
            </a>
        </div>
    </nav>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h4>Kontakt Info</h4>
                <p><strong>Adresse:</strong> {{ company.contact.address }}</p>
                <p><strong>Telefon:</strong> {{ company.contact.phone }}</p>
                <p><strong>E-Mail:</strong> {{ company.contact.email }}</p>
            </div>

            
        </div>
    </footer>

    <script>
        function toggleMenu() {
            var x = document.getElementById("navLinks");
            if (x.className === "nav-links") {
                x.className += " show";
            } else {
                x.className = "nav-links";
            }
        }
    </script>

</body>
</html>
