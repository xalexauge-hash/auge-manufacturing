---
layout: base.njk
title: Home
---

<section class="hero" style="background: linear-gradient(135deg, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.3) 100%), url('/public/images/cut_country.png') center/cover no-repeat; position: relative; min-height: 500px; display: flex; align-items: center; justify-content: center;">
    <div class="hero-text" style="text-align: center; color: white; z-index: 2;">
        <h1 style="font-size: 2.8rem; line-height: 1.2; margin-bottom: 1rem;">Lokale, zuverlässige und präzise<br>Frästechnik</h1>
        <p style="font-size: 1.2rem; margin-bottom: 2rem; font-weight: 300;">DACH-weit für Ihre Anforderungen</p>
        <a href="{{ company.contact.cta_link }}" class="btn-quote">JETZT ANFRAGE SENDEN</a>
    </div>
</section>

<section class="expertise">
    <h2>Unsere Expertise</h2>
    <div class="services-grid">
        
        {% for service in company.services %}
        <div class="service-card">
            <h3>{{ service.title }}</h3>
            <p>{{ service.description }}</p>
        </div>
        {% endfor %}

    </div>
</section>